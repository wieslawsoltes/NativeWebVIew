[Added] Added machine-readable diagnostics conformance summary artifact (`exit-code-contract-summary.json`) with per-scenario expected/actual exit-code outcomes and fingerprints.
[Changed] Extended conformance summary contract to include fingerprint baseline gate metadata (`enabled`, match status, mismatch count, comparison artifact paths).
[Changed] Added summary JSON contract validation checks in `validate-diagnostics-exit-code-contract.sh`.
[Docs] Updated diagnostics/CI/quickstart/prerequisites/README docs for machine-readable conformance summary usage.
