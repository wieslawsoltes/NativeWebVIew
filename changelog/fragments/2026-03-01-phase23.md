[Added] Added repository diagnostics fingerprint baseline (`ci/baselines/diagnostics-fingerprint-baseline.txt`) for conformance contract gating.
[Changed] Extended diagnostics exit-code conformance script with optional fingerprint baseline enforcement and current fingerprint artifact output.
[Added] Added `scripts/update-diagnostics-fingerprint-baseline.sh` helper for deterministic fingerprint baseline refresh.
[CI] CI/release conformance steps now enforce fingerprint baseline drift checks.
