[Added] Added stale-baseline and baseline-update status flags for diagnostics regression results.
[Changed] Extended diagnostics sample/script with `--require-baseline-sync` gating for resolved baseline entries.
[Added] Added `scripts/update-blocking-baseline.sh` to regenerate blocking diagnostics baselines.
[CI] Enforced baseline-sync hygiene in CI/release diagnostics gates.
[Docs] Documented baseline refresh workflow and stale-baseline policy updates.
