[Added] Added structured gate failure metadata (`gateFailures`) to diagnostics regression evaluation outputs.
[Changed] Unified gate failure messages/recommendations across JSON output, markdown formatter, and CLI error reporting.
[Changed] Extended diagnostics scripts and conformance automation to enforce structured gate failure metadata contract.
[Docs] Documented `gateFailures` fields for diagnostics report consumers.
